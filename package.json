{
  "name": "silver-lining-of-hope",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Website for Silver Lining of Hope, Inc — investigative advocacy for the missing, the murdered, and the unidentified.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource-variable/fraunces": "^5.1.0",
    "@fontsource-variable/public-sans": "^5.1.0",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
